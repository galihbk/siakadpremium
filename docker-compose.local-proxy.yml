services:
  local-proxy:
    image: caddy:2-alpine
    container_name: siakad-local-proxy
    restart: unless-stopped
    ports:
      - '80:80'
    volumes:
      - ./docker/proxy/Caddyfile:/etc/caddy/Caddyfile:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
